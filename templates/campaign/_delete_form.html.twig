<form method="post" action="{{ path('app_campaign_delete', {'idcamp': campaign.idcamp}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ campaign.idcamp) }}">
    <button class="btn btn-danger-custom btn-sm">Delete</button>
</form>
